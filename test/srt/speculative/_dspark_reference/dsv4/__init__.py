# DSpark V4 (DeepSeek-V4-Flash-DSpark) parity-oracle reference copy.
# Upstream source: lab/code/scratch/2026-06-29-spark/DeepSeek-V4-Flash-DSpark-inference/
#   model.py (DSparkAttention.forward @ ~752, get_dspark_topk_idxs @ ~744,
#   apply_rotary_emb @ ~238, precompute_freqs_cis @ ~206)
#   kernel.py (sparse_attn @ ~355).
# See sot_dspark_attention.py for the copy-vs-adapt notes.
